import {chromium} from 'file:///C:/Users/Miroslav%20Pavelek/.codex/skills/develop-web-game/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const browser=await chromium.launch({headless:true,args:['--use-gl=angle','--use-angle=swiftshader']});const page=await browser.newPage({viewport:{width:390,height:844},hasTouch:true});const errors=[];page.on('pageerror',e=>errors.push(String(e)));
await page.goto('http://localhost:3000');await page.waitForFunction(()=>!!window.render_game_to_text);await page.evaluate(()=>window.advanceTime(0));await page.screenshot({path:'output/variety/mobile-intro.png'});await page.getByRole('button',{name:'ENTER THE KEEP',exact:false}).click();await page.evaluate(()=>window.advanceTime(0));
const read=()=>page.evaluate(()=>JSON.parse(window.render_game_to_text()));const touch=await page.context().newCDPSession(page);await touch.send('Emulation.setTouchEmulationEnabled',{enabled:true,maxTouchPoints:5});
const right=await page.getByRole('button',{name:'Move right',exact:true}).boundingBox(),strike=await page.getByRole('button',{name:'STRIKE',exact:true}).boundingBox();
const rp={x:right.x+right.width/2,y:right.y+right.height/2,id:1},sp={x:strike.x+strike.width/2,y:strike.y+strike.height/2,id:2};
const before=await read();await touch.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[rp]});await page.evaluate(()=>window.advanceTime(120));let after=await read();console.log({modeBefore:before.mode,modeAfter:after.mode,before:before.player.x,after:after.player.x,errors});assert(after.player.x>before.player.x);
await touch.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[rp,sp]});await page.evaluate(()=>window.advanceTime(500));after=await read();console.log({attack:after.player.attackTime,mode:after.mode,errors});assert(after.player.attackTime>0);
await touch.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});await page.evaluate(()=>window.advanceTime(500));assert.equal((await read()).player.attackTime,0);assert.equal((await read()).player.velocity.x,0);
await page.getByRole('button',{name:'DASH',exact:true}).click();await page.evaluate(()=>window.advanceTime(100));assert((await read()).player.dashCooldown>0);await page.screenshot({path:'output/variety/mobile.png'});
await page.getByRole('button',{name:'Pause game'}).click();await page.screenshot({path:'output/variety/pause.png'});assert.equal((await read()).mode,'paused');const paused=await read();await page.evaluate(()=>window.advanceTime(5000));assert.equal((await read()).player.x,paused.player.x);await page.getByRole('button',{name:'RESUME JOURNEY',exact:false}).click();
await page.getByRole('button',{name:'Toggle fullscreen'}).click();await page.waitForFunction(()=>!!document.fullscreenElement);await page.getByRole('button',{name:'Toggle fullscreen'}).click();await page.waitForFunction(()=>!document.fullscreenElement);assert.deepEqual(errors,[]);console.log('Touch hold, simultaneous move, release, dash, pause/resume, fullscreen passed.');await browser.close();

